spring.application.name=Bchat
# Server port (optional, default 8080)
server.port=8080

# Actuator endpoints exposure (optional, for health/info)
management.endpoints.web.exposure.include=health,info

# Webhook token (read from environment variable TOKEN; empty if not set)
webhook.token=${TOKEN:}